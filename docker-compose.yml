version: '3.5'
services:
  dev:
    user: root
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./:/src/
    container_name: helix_fhir_profiles_dev
    working_dir: /src
    env_file:
      - docker.env